export type User = {
    _id: string;
    name: string;
    isAdmin: boolean;
    email: string;
    emailVerified: boolean;
    about: string;
    image: string;
    _createdAt: string;
  };
  
